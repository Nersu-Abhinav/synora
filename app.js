const agents=[['RESEARCH','Discovery & intelligence','Scans context, sources and constraints.','⌁'],['STRATEGIST','Planning & decomposition','Turns intent into measurable execution plans.','◈'],['BUILDER','Implementation','Produces code, artifacts and concrete changes.','◆'],['EXECUTOR','Action runner','Coordinates tools and completes operational steps.','➜'],['ANALYST','Evaluation & insight','Measures quality, risk and performance.','∿'],['ORCHESTRATOR','Mission control','Routes work, resolves dependencies and verifies outcomes.','✦']];
const state={view:'command',runs:JSON.parse(localStorage.getItem('synora_runs')||'[]'),settings:JSON.parse(localStorage.getItem('synora_settings')||'null')||{demo:true,telemetry:true,density:false},toast:'',running:false};
const nav=[['command','⌘','Command Center'],['fleet','◉','Agent Fleet'],['workflow','⇄','Workflows'],['analytics','◒','Analytics'],['architecture','⌘','Architecture'],['settings','⚙','Settings']];
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function app(){document.querySelector('#app').innerHTML=`<div class="app"><div class="shell"><aside class="side"><div class="brand"><div class="mark">S</div><div><b>SYNORA</b><small>AI WORKFORCE</small></div></div><nav class="nav">${nav.map(([id,ic,label])=>`<button class="${state.view===id?'active':''}" data-view="${id}"><strong>${ic}</strong><span>${label}</span></button>`).join('')}</nav><div class="status"><span class="dot"></span><b style="font-size:11px">SYSTEM OPERATIONAL</b><p>6 agents registered · ${state.runs.length} missions completed</p></div></aside><main class="main"><header class="top"><div class="crumb">SYNORA / <b>${nav.find(x=>x[0]===state.view)?.[2]||'Command Center'}</b></div><div class="topright"><span class="pill">● ${state.settings.demo?'DEMO TELEMETRY':'LIVE RUNTIME'}</span><div class="avatar">NA</div></div></header><section class="content">${renderView()}</section></main></div>${state.toast?`<div class="toast">${esc(state.toast)}</div>`:''}</div>`;bind();}
function renderView(){if(state.view==='command')return command();if(state.view==='fleet')return fleet();if(state.view==='workflow')return workflow();if(state.view==='analytics')return analytics();if(state.view==='architecture')return architecture();return settings();}
function command(){return `<div class="eyebrow">AI WORKFORCE / MISSION CONTROL</div><h1 class="title">Turn intent into<br><span style="color:#53dff6">intelligent action.</span></h1><p class="sub">SYNORA coordinates specialized agents that research, plan, build, execute and verify complex work — from one human command.</p><div class="grid heroGrid"><div class="panel heroPanel"><div class="heroCopy"><span class="pill">ORCHESTRATOR ONLINE</span><h2 style="font-size:28px;margin:24px 0 8px">What should SYNORA accomplish?</h2><p class="sub">Describe an outcome in natural language. The workforce will decompose the mission and return an auditable execution plan.</p><div class="command"><div class="composer"><textarea id="goal" placeholder="e.g. Research the market, design a launch strategy, and prepare an executive brief..."></textarea><button class="primary run" id="run">RUN MISSION</button></div></div></div></div><div class="panel"><div class="eyebrow">LIVE TELEMETRY</div><h3 style="font-size:20px;margin:12px 0 20px">Workforce health</h3><div class="sideStat"><div class="statline"><span>Active agents</span><b class="ok">06 / 06</b></div><div class="statline"><span>Mission success</span><b>98.4%</b></div><div class="statline"><span>Avg. response</span><b>1.8s</b></div><div class="statline"><span>Tasks coordinated</span><b>${Math.max(1248,state.runs.length*6+1248).toLocaleString()}</b></div></div><div class="metrics"><div class="metric"><strong>6</strong><span>Agents</span></div><div class="metric"><strong>${state.runs.length}</strong><span>Missions</span></div><div class="metric"><strong>98%</strong><span>Quality</span></div></div></div></div><div class="sectionHead"><div><h2>Specialized workforce</h2><p>Each agent has a focused role. The orchestrator coordinates the whole mission.</p></div><button class="secondary" data-view="fleet">VIEW FLEET →</button></div><div class="grid agentGrid">${agents.slice(0,3).map(agentCard).join('')}</div>`}
function agentCard(a){return `<div class="panel agent"><div class="agentTop"><div class="agentIcon">${a[3]}</div><span class="tag">READY</span></div><h3>${a[0]}</h3><p><b style="color:#b6c8da">${a[1]}</b><br>${a[2]}</p></div>`}
function fleet(){return `<div class="eyebrow">WORKFORCE / 06 AGENTS</div><h1 class="title" style="font-size:38px">Agent Fleet</h1><p class="sub">A coordinated digital workforce. Every specialist exposes a clear responsibility, state and operational role.</p><div class="grid agentGrid" style="margin-top:24px">${agents.map(agentCard).join('')}</div><div class="panel" style="margin-top:16px"><div class="sectionHead" style="margin:0 0 12px"><div><h2>Coordination protocol</h2><p>Orchestrator → specialists → verification → outcome</p></div><span class="pill">STRANDS PATTERN</span></div><div class="architecture"><div class="node"><strong>HUMAN INTENT</strong><span>Goal + constraints</span></div><div class="arrow">→</div><div class="node"><strong>ORCHESTRATOR</strong><span>Routing + memory</span></div><div class="arrow">→</div><div class="node"><strong>SPECIALISTS</strong><span>Parallel execution</span></div></div></div>`}
function workflow(){return `<div class="eyebrow">EXECUTION / MISSION LOG</div><h1 class="title" style="font-size:38px">Workflow Studio</h1><p class="sub">Inspect missions, execution stages and final outcomes. Demo missions are stored locally so the workspace survives refreshes.</p><div class="panel" style="margin-top:24px">${state.runs.length?`<table class="table"><thead><tr><th>Mission</th><th>Agents</th><th>Duration</th><th>Status</th></tr></thead><tbody>${state.runs.slice().reverse().map(r=>`<tr><td style="max-width:520px">${esc(r.goal)}</td><td>06</td><td>${r.duration}ms</td><td class="ok">● COMPLETED</td></tr>`).join('')}</tbody></table>`:`<div class="empty">No missions yet. Go to Command Center and run your first mission.</div>`}</div>`}
function analytics(){let n=state.runs.length;return `<div class="eyebrow">OBSERVABILITY / PERFORMANCE</div><h1 class="title" style="font-size:38px">Analytics</h1><p class="sub">Mission-level signals designed for evaluating an agentic system, not just a chatbot.</p><div class="grid" style="grid-template-columns:repeat(3,1fr);margin-top:24px"><div class="panel"><span class="eyebrow">SUCCESS RATE</span><strong style="font-size:34px;display:block;margin-top:14px">98.4%</strong><p class="sub">Stable across ${Math.max(n,1)} recorded mission${n===1?'':'s'}.</p></div><div class="panel"><span class="eyebrow">THROUGHPUT</span><strong style="font-size:34px;display:block;margin-top:14px">1,248</strong><p class="sub">Coordinated tasks processed.</p></div><div class="panel"><span class="eyebrow">QUALITY SCORE</span><strong style="font-size:34px;display:block;margin-top:14px">94 / 100</strong><p class="sub">Outcome verification signal.</p></div></div><div class="panel" style="margin-top:16px"><div class="sectionHead" style="margin-top:0"><div><h2>Agent utilization</h2><p>Relative workload distribution in the current demonstration.</p></div></div>${agents.map((a,i)=>`<div style="margin:16px 0"><div style="display:flex;justify-content:space-between;font-size:11px;margin-bottom:7px"><span>${a[0]}</span><b>${88-i*6}%</b></div><div class="bar"><i style="width:${88-i*6}%"></i></div></div>`).join('')}</div>`}
function architecture(){return `<div class="eyebrow">SYSTEM / ARCHITECTURE</div><h1 class="title" style="font-size:38px">Architecture</h1><p class="sub">A supervisor-style multi-agent topology designed to keep humans in control while specialists execute focused work.</p><div class="panel" style="margin-top:24px"><div class="architecture" style="grid-template-columns:1fr auto 1fr auto 1fr"><div class="node"><strong>USER</strong><span>Natural-language intent</span></div><div class="arrow">→</div><div class="node" style="border-color:#2a8296"><strong>ORCHESTRATOR</strong><span>Routing · planning · verification</span></div><div class="arrow">→</div><div class="node"><strong>AGENT TOOLS</strong><span>Research · build · execute · analyze</span></div></div><div style="height:30px;border-left:1px dashed #28536a;margin:0 auto"></div><div class="grid agentGrid">${agents.slice(0,3).map(agentCard).join('')}</div><div style="margin:24px auto 0;max-width:760px;border:1px dashed #28536a;border-radius:12px;padding:14px;text-align:center;color:#7890a7;font-size:11px">AWS Bedrock + Strands Agents SDK integration point · human approval remains the control boundary</div></div>`}
function settings(){return `<div class="eyebrow">SYSTEM / CONTROL PLANE</div><h1 class="title" style="font-size:38px">Settings</h1><p class="sub">Tune the operating mode without losing the mission history.</p><div class="settings" style="margin-top:24px"><div class="panel"><h3 style="margin-top:0">Runtime</h3>${setting('demo','Demo fallback','Use deterministic local execution when AWS credentials are not configured.')}${setting('telemetry','Motion & telemetry','Show live-status cues and execution telemetry.')}${setting('density','Compact density','Reduce spacing for smaller screens.')}</div><div class="panel"><h3 style="margin-top:0">Guardrails</h3><div class="setting"><span>Human approval boundary</span><b class="ok">ENABLED</b></div><div class="setting" style="margin-top:10px"><span>Agent count</span><b>06</b></div><div class="setting" style="margin-top:10px"><span>Data persistence</span><b>LOCAL</b></div></div></div><div class="panel" style="margin-top:16px"><h3 style="margin-top:0">About SYNORA</h3><p class="sub">SYNORA is built as an auditable AI workforce: one intent, multiple specialists, explicit orchestration and a verifiable outcome.</p></div>`}
function setting(key,title,desc){return `<div class="setting" style="margin-top:10px"><div><span>${title}</span><small style="display:block;color:#647990;font-size:10px;margin-top:4px">${desc}</small></div><button class="toggle ${state.settings[key]?'on':''}" data-setting="${key}"><i></i></button></div>`}
function bind(){document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{state.view=b.dataset.view;app()});document.querySelectorAll('[data-setting]').forEach(b=>b.onclick=()=>{let k=b.dataset.setting;state.settings[k]=!state.settings[k];localStorage.setItem('synora_settings',JSON.stringify(state.settings));app()});const run=document.querySelector('#run');if(run)run.onclick=runMission}
async function runMission(){const input=document.querySelector('#goal');const goal=input?.value.trim();if(!goal){notify('Describe the mission first.');return}state.running=true;app();const started=Date.now();let result=null;try{const res=await fetch('/api/orchestrate',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({goal})});if(res.ok)result=await res.json()}catch{}const duration=Date.now()-started;state.runs.push({goal,duration:Math.max(duration,820),result});state.runs=state.runs.slice(-20);localStorage.setItem('synora_runs',JSON.stringify(state.runs));state.running=false;state.view='workflow';state.toast='Mission completed — workflow and outcome recorded.';app();setTimeout(()=>{state.toast='';app()},2600)}
function notify(x){state.toast=x;app();setTimeout(()=>{state.toast='';app()},2200)}
app();
